# Required: Choose a deployment environment name
environment = "dev" # Use a simple name like "dev", "test", or "prod"

# Optional: Change AWS region if needed (defaults to "us-west-1" if not specified)
region = "us-west-1"

# Advanced: Only needed if using custom form adapter configurations
# textract_form_adapters_env_var_mapping = {
#   # You probably don't need to set this for your first deployment
# }
